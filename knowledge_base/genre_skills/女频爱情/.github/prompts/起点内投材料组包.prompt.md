---
name: 起点内投材料组包
agent: 小说作者
description: '用于把女频爱情作品整理成起点内投可用的版本化材料包（30000字连续正文 + 总大纲〈最低5000字〉+ 第1卷卷纲 + 简介），并默认吸收最新版起点评估结论。作者侧内部核对文件如有生成，不随稿发送。'
---

# 起点内投材料组包

## 题材技能路由（强制）

正式执行本 Prompt 时，必须先加载：`女频爱情-组包投稿材料`。

若当前作品尚未完成起点向评估，也应补充调用 `女频爱情-平台签约评估框架`；若要同步改写简介，则补充调用 `女频爱情-撰写内容简介`。

Prompt 不得绕过题材 Skill 直接裸用组包规则。

## 本 Prompt 只保留的职责

- 作为女频爱情作品“起点内投材料组包”的统一入口
- 保留最小输入、默认联动链路、落盘要求与聊天侧最小回执
- 把主体规则路由到 `女频爱情-组包投稿材料`

## 最小输入

1. 作品名 / 目标投放名
2. 起点内投目标版本的题材定位（一句话）
3. 目标正文来源：默认优先读取可组成至少 `30000` 字连续提交正文的开篇连续材料
4. 总纲 / 分卷 / 前十章节拍，至少命中其一
5. 主角、题材机制、核心卖点
6. 当前简介材料是否已有
7. 若已有：最新版起点评估主报告 / 概率结论 / 最小修复闭环

## 最低执行要求

- 必须优先读取并吸收起点评估研究资料与既有起点评估结论。
- 若当前没有可用起点评估结论，必须先补调 `女频爱情-平台签约评估框架`。
- 若当前尚未达到起点内投统一提交线（`30000` 字连续正文 + `内投版总大纲〈最低5000字〉` + `内投版第1卷卷纲` + `内投版简介`），必须显眼标成“缺件预备版”或“可投包预备版”；不得写成成熟内投包。
- 除非用户明确要求只草拟不落文件，否则必须写入仓库。
- 默认应写入：`投稿材料/起点内投/《作品名》/vYYYY-MM-DD-rN/`，并同步更新 `当前版本索引.md`。
- 对外交付件只包含：`内投版简介`、`内投版正文`、`内投版总大纲`、`内投版第 1 卷卷纲`。
- 若生成 `组包自检`、`版本说明`、`模板字段覆盖表` 等作者侧内部文件，必须明确标记为“不随稿发送”。
- 对外交付件中禁止出现任何自我评价、自吹自擂、当前最强 / 最弱一环、残留风险点评、`建议现在内投`、`可以直接进入编辑内投` 等替编辑做判断的内容。

## 默认交付

- 内投版简介
- 内投版正文（至少 `30000` 字连续正文）
- 内投版总大纲（最低 `5000` 字）
- 内投版第 `1` 卷卷纲

作者侧内部文件（如生成，不随稿发送）：

- 组包自检

## 一句话提醒

这份 Prompt 现在是**女频爱情入口层**；真正的组包骨架、落盘规则与起点女频裁判口径，都必须继续到对应 `女频爱情-*` Skill 中读取并执行。
