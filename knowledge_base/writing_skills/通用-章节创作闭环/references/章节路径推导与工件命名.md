# 章节路径推导与工件命名

## 默认平台与默认正文目录

- 除非用户明确指定其它平台版本，否则默认写的是`起点中文网原始章节版本`。
- 除非用户明确给出其它目标路径，否则 `CHAPTER_FILE` 默认落在工作区根目录下的`小说正文/`。
- 当用户只给章节编号、标题、卷别 / 分卷信息而未显式给出正文路径时，必须优先把 `CHAPTER_FILE` 推断到`小说正文/`对应目录，而不是把番茄、豆瓣、知乎、微信订阅号、GoodNovel、My Fiction、WebNovel 或出版社目录当默认正文落点。

## Step 0 初始化要求

- 必须检查 `OUTLINE_FILE` 是否存在且可读。
- 若 `CHAPTER_FILE` 所在目录不存在，则创建目录，但不要预写正文内容。
- 必须根据 `CHAPTER_FILE` 生成：
  - `CONTROL_CARD_FILE`（由 `通用-生成章节控制卡` 消费）
  - `REVIEW_REPORT_FILE`
  - `READER_NOTE_FILE`
  - `BOOK_REVIEW_FILE`
- 批量模式下，还应为每一章派生对应工件，并建立 `CONTINUITY_LEDGER_FILE` 默认位置。

## 章节编号与目录推导

- `第X部`：优先从 `CHAPTER_FILE` 前两级目录中的分部目录名提取开头数字 `X`
- `第Y卷`：优先从分卷目录名中提取 `A.B.` 里的 `B`
- `第X.Y.Z章`：优先从章节文件名中提取 `X.Y.Z`
- 对 `REVIEW_REPORT_FILE` 而言，若无法稳定提取分部 / 分卷，不得回退到 `审阅意见/未分类/` 或 `审阅意见/` 根目录；必须先从 `OUTLINE_FILE`、分卷目录或目标章节路径补齐定位信息，再生成报告路径

## 推荐命名

- 正文：`小说正文/第X部目录/第Y卷目录/X.Y.Z 标题.md`
- 章节控制卡：`写作控制卡/第X部/第Y卷/控制卡_第X.Y.Z章_<YYYY-MM-DD>.md`
  - 降级（无部卷）：`写作控制卡/未分类/控制卡_第X.Y.Z章_<YYYY-MM-DD>.md`
- 审阅报告（有分部）：`审阅意见/第X部/第Y卷/章节审阅报告_第X部_第Y卷_第Z章_<YYYY-MM-DD>.md`
- 审阅报告（无分部）：`审阅意见/第X卷/章节审阅报告_第X卷_第Y章_<YYYY-MM-DD>.md`
- 阅读笔记：`阅读笔记/第X部/第Y卷/阅读笔记_第X.Y.Z章_<YYYY-MM-DD>.md`
- 分章书评：`书评/第X部/第Y卷/<章节文件名去扩展名>_书评_<YYYY-MM-DD>.md`

## 未分类兜底

若无法稳定提取 `第X部` 或 `第Y卷`，则以下未分类兜底**只适用于非章节审阅报告工件**：

- `阅读笔记/未分类/`
- `书评/未分类/`

章节审阅报告必须等部 / 卷定位补齐后再落盘；不要用 `未分类/` 掩盖路径信息缺失。

## 路径合规门禁（强制）

**目的**：防止执行时将审阅报告、阅读笔记、书评等派生产物错误地写入 `小说正文/` 目录。

### 派生路径推导规则

生成 `CONTROL_CARD_FILE` / `REVIEW_REPORT_FILE` / `READER_NOTE_FILE` / `BOOK_REVIEW_FILE` / `CONTINUITY_LEDGER_FILE` 时，必须遵循以下推导逻辑：

1. 优先从 `CHAPTER_FILE` 路径中提取 `第X部` 和 `第Y卷` 信息（规则参见"章节编号与目录推导"）
2. 根据"推荐命名"中的格式生成完整目标路径
3. **不得将 `CHAPTER_FILE` 的所在目录直接作为其他工件的基目录**——即使 CHAPTER_FILE 在 `小说正文/第1卷/` 下，REVIEW_REPORT_FILE 也必须去 `审阅意见/`，而不是沿用到 `小说正文/审阅报告/`

### 合法性校验

派生路径生成后，必须逐项校验：

| 工件 | 目标路径必须以此开头 | 不合规时的处置 |
| --- | --- | --- |
| CONTROL_CARD_FILE | `写作控制卡/` | **阻断**：控制卡不得在 `小说正文/` 等目录下 |
| REVIEW_REPORT_FILE | `审阅意见/` | **阻断**：回退修正路径，不得以任何理由写往其他目录 |
| READER_NOTE_FILE | `阅读笔记/` | **阻断**：回退修正路径，不得以任何理由写往其他目录 |
| BOOK_REVIEW_FILE | `书评/` | **阻断**：回退修正路径，不得以任何理由写往其他目录 |
| CONTINUITY_LEDGER_FILE | `SOP执行日志/` | **阻断**：回退修正路径，不得以任何理由写往其他目录 |

### 常见错误示例（禁止）

```text
CONTROL_CARD_FILE  = 小说正文/控制卡_...md               ← ❌ 控制卡不得在 小说正文/ 下
REVIEW_REPORT_FILE = 小说正文/章节审阅报告_...md       ← ❌ 审阅报告不得在 小说正文/ 下
READER_NOTE_FILE   = 小说正文/阅读笔记_...md            ← ❌ 阅读笔记不得在 小说正文/ 下
BOOK_REVIEW_FILE   = 小说正文/书评_...md                ← ❌ 书评不得在 小说正文/ 下
CONTINUITY_LEDGER  = 小说正文/连续性台账_...md           ← ❌ 台账不得在 小说正文/ 下
```

### 正确示例

```text
CONTROL_CARD_FILE  = 写作控制卡/第1部/第2卷/控制卡_1.2.3章_2026-06-11.md                 ✓
REVIEW_REPORT_FILE = 审阅意见/第1部/第2卷/章节审阅报告_第1部_第2卷_第3章_2026-06-11.md  ✓
READER_NOTE_FILE   = 阅读笔记/第1部/第2卷/阅读笔记_1.2.3章_2026-06-11.md                ✓
BOOK_REVIEW_FILE   = 书评/第1部/第2卷/1.2.3 深夜来访_书评_2026-06-11.md                   ✓
CONTINUITY_LEDGER  = SOP执行日志/撰写章节/连续章台账/连续性台账_第1部_第2卷_2026-06-11.md    ✓
```

### 题材包装层的路径路由责任

题材级的章节创作闭环包装 Skill（如 `都市悬疑-章节创作闭环`）在路由到本 references 时，还必须：

- 检查其题材项目的根目录是否存在 `小说正文/`、`写作控制卡/`、`审阅意见/`、`阅读笔记/`、`书评/`、`SOP执行日志/` 等目录
- 若执行时发现 `审阅意见/` 等目录不存在，**不得静默回退到把报告丢进 `小说正文/`**，而是应当主动创建缺失目录
- 在批量模式的初始化相位中，先全量创建所有必需目录结构，再开始逐章执行

## 连续性台账默认位置

- 优先：`SOP执行日志/撰写章节/连续章台账/连续性台账_第X部_第Y卷_<YYYY-MM-DD>.md`
- 若无法推导部卷：`SOP执行日志/撰写章节/连续章台账/连续性台账_未分类_<YYYY-MM-DD>.md`

台账只记录结构化信息，不得粘贴正文段落。

## 管理编号禁入读者文本

- `C01`、`FB-01`、`SL-02`、`R3`、`M0` 这类只供作者在大纲、台账、审阅报告、章节后记里管理设计的编号，不得直接写进章节正文、章引语或 `## 作者有话说`。
- 若需要让读者感知对应信息，必须改写成自然叙事、对白、动作、人物记忆或剧情内真实存在的证据载体。
- 案件编号、回执号、监控点位编号、时间码、文书编号等剧情内真实编号不在此禁令内。
